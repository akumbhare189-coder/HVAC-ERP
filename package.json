{
  "name": "hvac-erp-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Stack HVAC ERP Enterprise Operations Web Application",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm install && npx prisma generate && npx prisma db push --skip-generate && npm run build && cd ../frontend && npm install && npm run build",
    "seed": "cd backend && npm run db:seed",
    "start": "cd backend && node dist/index.js"
  }
}
