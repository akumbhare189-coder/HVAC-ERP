services:
  - type: web
    name: hvac-erp-app
    env: node
    plan: free
    buildCommand: npm run build && npm run seed
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
